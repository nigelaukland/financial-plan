VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Set to "true" to skip sign-in when running `npm run dev` and use
# localStorage instead of the real Supabase plan_data table. Never active in
# a production build regardless of this value.
VITE_DEV_BYPASS_AUTH=
